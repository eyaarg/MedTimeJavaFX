<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/17.0.2-ea"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="esprit.fx.controllers.SuggestionController"
      prefWidth="560" prefHeight="620"
      style="-fx-background-color: #f3f4f6;">

    <!-- Header -->
    <VBox style="-fx-background-color: linear-gradient(to right, #4f46e5 0%, #7c3aed 100%);
                 -fx-padding: 24 28;">
        <Label text="💡 Suggestion de Créneaux"
               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
        <Label text="Trouvez le meilleur créneau disponible automatiquement"
               style="-fx-font-size: 12px; -fx-text-fill: rgba(255,255,255,0.85); -fx-padding: 4 0 0 0;"/>
    </VBox>

    <!-- Formulaire de recherche -->
    <VBox spacing="14"
          style="-fx-background-color: white; -fx-padding: 20;
                 -fx-border-color: #e5e7eb; -fx-border-width: 0 0 1 0;">

        <HBox spacing="14" alignment="CENTER_LEFT">
            <!-- Médecin -->
            <VBox spacing="6" HBox.hgrow="ALWAYS">
                <Label text="👨‍⚕️ Médecin"
                       style="-fx-font-weight: 700; -fx-font-size: 13px; -fx-text-fill: #374151;"/>
                <ComboBox fx:id="comboDocteur" maxWidth="Infinity"
                          style="-fx-background-color: #f9fafb; -fx-border-color: #e5e7eb;
                                 -fx-border-radius: 8; -fx-background-radius: 8;
                                 -fx-padding: 8; -fx-font-size: 13px;"/>
            </VBox>

            <!-- Plage horaire -->
            <VBox spacing="6">
                <Label text="🕐 Plage horaire"
                       style="-fx-font-weight: 700; -fx-font-size: 13px; -fx-text-fill: #374151;"/>
                <ComboBox fx:id="comboPlage" prefWidth="140"
                          style="-fx-background-color: #f9fafb; -fx-border-color: #e5e7eb;
                                 -fx-border-radius: 8; -fx-background-radius: 8;
                                 -fx-padding: 8; -fx-font-size: 13px;"/>
            </VBox>
        </HBox>

        <HBox spacing="14" alignment="BOTTOM_LEFT">
            <!-- Semaine -->
            <VBox spacing="6" HBox.hgrow="ALWAYS">
                <Label text="📅 Semaine souhaitée"
                       style="-fx-font-weight: 700; -fx-font-size: 13px; -fx-text-fill: #374151;"/>
                <DatePicker fx:id="datePickerSemaine" maxWidth="Infinity"
                            style="-fx-background-color: #f9fafb; -fx-border-color: #e5e7eb;
                                   -fx-border-radius: 8; -fx-background-radius: 8;"/>
            </VBox>

            <!-- Bouton -->
            <Button fx:id="btnSuggerer" text="🔍 Trouver des créneaux"
                    onAction="#lancerSuggestion"
                    style="-fx-background-color: #4f46e5; -fx-text-fill: white;
                           -fx-font-weight: 700; -fx-padding: 12 20;
                           -fx-background-radius: 8; -fx-cursor: hand;
                           -fx-font-size: 13px;"/>
        </HBox>

        <!-- Status -->
        <Label fx:id="labelStatus" text=""
               wrapText="true"
               style="-fx-font-size: 13px; -fx-text-fill: #6b7280;"/>
    </VBox>

    <!-- Résultats -->
    <ScrollPane VBox.vgrow="ALWAYS" fitToWidth="true"
                style="-fx-background-color: transparent; -fx-background: #f3f4f6;">
        <padding><Insets top="16" right="20" bottom="16" left="20"/></padding>
        <VBox fx:id="suggestionsContainer" spacing="4"/>
    </ScrollPane>

    <!-- Footer -->
    <HBox alignment="CENTER_RIGHT"
          style="-fx-background-color: white; -fx-padding: 14 20;
                 -fx-border-color: #e5e7eb; -fx-border-width: 1 0 0 0;">
        <Label text="💡 Créneaux de 30 min — Algorithme de disponibilité automatique"
               style="-fx-font-size: 11px; -fx-text-fill: #9ca3af;"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button fx:id="btnFermer" text="✖ Fermer" onAction="#fermer"
                style="-fx-background-color: #6b7280; -fx-text-fill: white;
                       -fx-font-weight: 600; -fx-padding: 10 24;
                       -fx-background-radius: 8; -fx-cursor: hand;"/>
    </HBox>

</VBox>
